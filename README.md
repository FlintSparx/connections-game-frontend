# Connections Game

A web app where players connect tiles or items according to game rules.

Built with React and Vite. Supports hot module replacement for fast development.

## Getting Started

Run `npm install` then `npm run dev` to start the development server.
